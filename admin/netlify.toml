[build]
  command = "npm run build"
  publish = "build"

[build.environment]
  # API root for the admin site; set this to your backend URL (e.g. Render/Heroku)
  REACT_APP_API_URL = "https://vintage-fades.onrender.com"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
