[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  # API root for the admin site; set this to your backend URL (e.g. Render/Heroku)
  VITE_API_URL = "https://vintage-fades.onrender.com"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
